{
  "name": "axolotl-os",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces && rm -rf dist && cp -r frontend/dist dist"
  }
}
